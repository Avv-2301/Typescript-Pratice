const obj: { name: string; model?: number } = {
  name: "bmw",
};
obj.model = 2002

console.log(obj)


//to change keys into string format
const nameAge:{[index:string]: number} ={};
nameAge.avv = 1;
console.log(nameAge)