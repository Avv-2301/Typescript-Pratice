enum direction{
    north = 1, //can change so it start from 1 and so on 
    west = "west",      //can also assign any type of value to it
    east = "east",
    south = "south"
}

let curr = direction;
console.log(curr);